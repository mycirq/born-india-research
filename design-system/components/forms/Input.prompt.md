Text field. The label is mono uppercase 11px, sitting above the field — never a floating placeholder label.

```jsx
<Input label="Budget" prefix="₹" placeholder="2,00,00,000" hint="Ballpark is fine." />
```

Focus draws a laterite border plus `--shadow-focus`. Errors replace the hint text.
