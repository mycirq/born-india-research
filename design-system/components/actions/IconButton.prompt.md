A square hairline button for a lone icon — menu toggles, downloads, close.

```jsx
<IconButton label="Download brief"><i data-lucide="download"></i></IconButton>
```

Always pass `label`. Hover fills with `--paper-sunk`; the border never changes colour.
